<Page x:Class="Bei_Daniel.View.Pages.MonthyInvoiceOverview"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Bei_Daniel.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MonthyInvoiceOverview">

    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Select Date Range for Invoice" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Start Date:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <DatePicker SelectedDate="{Binding StartDatePicker, Mode=TwoWay}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="End Date:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <DatePicker SelectedDate="{Binding EndDatePicker, Mode=TwoWay}" />
            </StackPanel>

            <TextBlock Text="Restaurant Information" FontSize="16" FontWeight="Bold" Margin="0,10,0,10"/>
            <TextBlock Text="{Binding Restaurant.Name, StringFormat='Name: {0}'}" />
            <TextBlock Text="{Binding Restaurant.Address, StringFormat='Address: {0}'}" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="30" >
            <Button
                        Content="🖨 Monat Drucken" 
                        Height="36"
                        Background="#E74C3C" 
                        Foreground="White" 
                        FontWeight="SemiBold"
                        FontSize="13"
                        BorderThickness="0"
                        Padding="16,8,16,8"
                        Cursor="Hand"
                        ToolTip="Ganzen Monat drucken"
                        Command="{Binding PrintIntervalSelectedInvoice}" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Page>