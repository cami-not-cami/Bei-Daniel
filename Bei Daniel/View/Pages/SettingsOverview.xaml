<Page x:Class="Bei_Daniel.View.Pages.SettingsOverview"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Bei_Daniel.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SettingsOverview">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Invoice Number Settings" 
                   FontSize="18" 
                   FontWeight="SemiBold" 
                   Margin="0,0,0,10"
                   Foreground="White"
                   />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Invoice Number:" 
                       VerticalAlignment="Center" 
                       Margin="0,0,10,0"
                       Foreground="White"/>
            <TextBox Text="{Binding InvoiceNumberInput, UpdateSourceTrigger=PropertyChanged}" 
                     Width="200" 
                     VerticalAlignment="Center"/>
            <Button Content="Set Invoice Number" 
                    Command="{Binding SetInvoiceStartNumber}" 
                    Margin="10,0,0,0" 
                    Padding="10,5"/>
        </StackPanel>
    </Grid>
</Page>
